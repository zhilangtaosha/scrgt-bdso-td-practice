apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: elasticsearchdb
  name: elasticsearchdb
spec:
  port:
    targetPort: http
  tls:
    termination: edge
  to:
    kind: Service
    name: elasticsearchdb-master
    weight: 100
  wildcardPolicy: None