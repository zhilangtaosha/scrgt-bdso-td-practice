apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: elasticsearch
  name: elasticsearch
spec:
  port:
    targetPort: http
  to:
    kind: Service
    name: elasticsearch-master
    weight: 100
  wildcardPolicy: None